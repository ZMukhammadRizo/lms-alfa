// Student routes section to be inserted in App.tsx or routes/index.tsx
// Add this new route after the existing assignment route
<Route path='assignments/:assignmentId' element={<SingleAssignment />} />